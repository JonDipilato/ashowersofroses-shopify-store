<section class="section-shell">
  <div class="page-width page-template">
    <p class="section-kicker">404</p>
    <h1 class="page-template__title">{{ 'templates.404.title' | t }}</h1>
    <p>{{ 'templates.404.subtext' | t }}</p>
    <div>
      <a class="btn btn-primary" href="{{ routes.root_url }}">Return home</a>
      <a class="btn btn-secondary" href="{{ routes.all_products_collection_url }}">Browse all products</a>
    </div>
  </div>
</section>

{% schema %}
{
  "name": "Main 404"
}
{% endschema %}
